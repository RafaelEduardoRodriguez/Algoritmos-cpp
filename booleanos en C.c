#include <stdio.h>
#include <stdbool.h>

//typedef enum {false, true} bool;

int main(void) {
	
	bool ans;
	ans = false;
	if(ans) printf("%d is true\n", (int)ans);
	else printf("%d is false\n", (int)ans);
	
	return 0;
}
